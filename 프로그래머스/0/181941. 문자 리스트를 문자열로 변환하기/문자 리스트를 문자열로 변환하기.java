class Solution {
    public String solution(String[] arr) {
        String answer = "";
        for (int ndx=0; ndx<arr.length; ndx++) {
        	answer += arr[ndx];
        }
        return answer;
    }
}